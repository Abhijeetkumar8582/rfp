export const metadata = {
  title: "Team Directory | Kiza",
  description: "View and manage team members, access levels, and activity in Kiza.",
};

export default function TeamDirectoryLayout({ children }) {
  return children;
}
