export const metadata = {
  title: "Settings | Kiza",
  description: "Configure Kiza account, team, and system settings.",
};

export default function SettingsLayout({ children }) {
  return children;
}
