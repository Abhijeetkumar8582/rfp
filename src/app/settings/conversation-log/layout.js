export const metadata = {
  title: "Conversation Log | Kiza Settings",
  description: "Review AI conversations, queries, and responses to monitor performance in Kiza.",
};

export default function ConversationLogLayout({ children }) {
  return children;
}
