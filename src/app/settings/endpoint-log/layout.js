export const metadata = {
  title: "Endpoint Log | Kiza Settings",
  description: "Inspect API endpoint request and response history in Kiza.",
};

export default function EndpointLogLayout({ children }) {
  return children;
}
