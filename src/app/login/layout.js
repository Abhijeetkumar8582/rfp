export const metadata = {
  title: "Login | Kiza",
  description: "Sign in to Kiza to manage RFP documents, search, and audit activity.",
};

export default function LoginLayout({ children }) {
  return children;
}
